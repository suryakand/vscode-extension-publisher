{
  "name": "vscode-extension-publisher",
  "version": "1.0.0",
  "description": "Github Action for publishing Visual Studio Code (VS Code) Extension to Microsoft marketplace",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "author": "",
  "license": "ISC",
  "scripts": {
    "test": "jest --config jest.config.js",
    "test:watch": "jest --config jest.config.js --watch",
    "test:coverage": "jest --config jest.config.js --coverage",
    "build": "tsc",
    "clean": "rimraf dist",
    "test:action": "act -W .github/workflows/test-action.yml"
  },
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "@types/node": "^20.6.0",
    "@types/yaml": "^1.9.7",
    "jest": "^29.6.4",
    "rimraf": "^5.0.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2",
    "yaml": "^2.3.2"
  }
}
